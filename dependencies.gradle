ext {
	//Start dagger region
	dagger_version = "2.16"
	dagger = [
			base    : "com.google.dagger:dagger:${dagger_version}",
			compiler: "com.google.dagger:dagger-compiler:${dagger_version}"
	]
	//End dagger region
	//Start moshi region
	moshi_version = "1.7.0"
	moshi = [
	        base: "com.squareup.moshi:moshi:${moshi_version}",
			kotlin: "com.squareup.moshi:moshi-kotlin-codegen:${moshi_version}"
	]
	//End moshi region
}